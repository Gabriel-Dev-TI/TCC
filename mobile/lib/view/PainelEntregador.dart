import 'package:flutter/material.dart';

class Entregador extends StatelessWidget {
  const Entregador({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
